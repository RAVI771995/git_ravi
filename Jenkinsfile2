pipeline {
    agent any

    stages {
        stage('Run SSH Command on Remote Server') {
            steps {
                
               sshagent (credentials: ['SSH_PEM']) {
                 sh ' ls -lrt'
                }
                
                
                
            }
        }

        
    }
}
