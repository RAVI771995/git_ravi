pipeline {
    agent any

    stages {
        stage('Run SSH Command on Remote Server') {
            steps {
                script {
                    sshagent(['SSH_PEM']) {
                        // Your SSH commands go here
                        sh 'ssh -i SSH_PEM ubuntu@65.2.184.189 '
                    }
                }
            }
        }

        // Add more stages as needed
    }
}
