pipeline{
    agent any 

    stages {
        stage ('build') {
             steps {
                sh '''
                pwd
                ls -lrt
                '''
            }
        }
         stage ('testing') {
            steps {
                sh '''
                df -h 
                '''
            }
        }
    }    
    
}
